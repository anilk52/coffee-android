<resources>
    <!-- Uygulamanın ana teması -->
    <style name="Theme.BdinoCoffee" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/coffee_deep_green</item>
        <item name="colorSecondary">@color/coffee_latte_brown</item>

        <!-- Arka plan ve sistem barlar -->
        <item name="android:windowBackground">@color/coffee_beige</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Splash teması -->
    <style name="Theme.BdinoCoffee.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/coffee_beige</item>
    </style>
</resources>