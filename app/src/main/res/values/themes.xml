<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Uygulama ana teması -->
    <style name="Theme.BdinoCoffee" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- İstersen burada renk/öğe override edebilirsin -->
    </style>

    <!-- Splash teması: core-splashscreen öznitelikleri -->
    <style name="Theme.BdinoCoffee.Splash" parent="Theme.SplashScreen">
        <!-- Arka plan rengi (sade ve güvenli) -->
        <item name="windowSplashScreenBackground">#FFFFFFFF</item>

        <!-- Logo/anim ikon (elinde yoksa @mipmap/ic_launcher kullan) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_placeholder_logo</item>

        <!-- Splash sonrası geçilecek tema -->
        <item name="postSplashScreenTheme">@style/Theme.BdinoCoffee</item>
    </style>

</resources>