<resources>
    <!-- Uygulama teması -->
    <style name="Theme.Coffee" parent="Theme.AppCompat.Light.NoActionBar" />

    <!-- Bazı kütüphaneler/şablonlar Body adını bekliyor; Body1'e köprü kuralım -->
    <style name="TextAppearance.AppCompat.Body" parent="TextAppearance.AppCompat.Body1"/>
    <!-- Eski layout’larda kullanılabilecek basit bir metin stili (opsiyonel) -->
    <style name="Text" parent="TextAppearance.AppCompat.Body1"/>
</resources>
