<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Android 12+ SplashScreen temasıyla uyumlu ad -->
    <style name="Theme_Coffee_Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <!-- Buraya PNG logonu ekleyince: @drawable/ic_logo -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Splash bittiğinde geçilecek ana tema -->
        <item name="postSplashScreenTheme">@style/Theme.BdinoCoffee</item>
    </style>

</resources>