<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/mainContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Tarif görseli / bardak -->
        <ImageView
            android:id="@+id/imgHero"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_foreground"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Başlık -->
        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Tarif Başlığı"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            app:layout_constraintTop_toBottomOf="@id/imgRecipe"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Açıklama -->
        <TextView
            android:id="@+id/txtDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Tarif açıklaması buraya gelecek..."
            android:textSize="14sp"
            android:textColor="@android:color/darker_gray"
            app:layout_constraintTop_toBottomOf="@id/txtTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Ölçü label -->
        <TextView
            android:id="@+id/txtMeasureLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Ölçü:"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/txtDescription"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Ölçü değeri -->
        <TextView
            android:id="@+id/txtMeasure"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="18 g kahve, 36 g shot"
            app:layout_constraintTop_toTopOf="@id/txtMeasureLabel"
            app:layout_constraintStart_toEndOf="@id/txtMeasureLabel"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Boyut label -->
        <TextView
            android:id="@+id/txtSizeLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Bardak boyutu:"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/txtMeasureLabel"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Boyut değeri (T / S / M / L / XL) -->
        <TextView
            android:id="@+id/txtSize"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="M – 240 ml"
            app:layout_constraintTop_toTopOf="@id/txtSizeLabel"
            app:layout_constraintStart_toEndOf="@id/txtSizeLabel" />

        <!-- Barista ipucu label -->
        <TextView
            android:id="@+id/txtTipLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Barista ipucu:"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/txtSizeLabel"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Barista ipucu -->
        <TextView
            android:id="@+id/txtTip"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Shot alırken toplam süreyi 25–30 sn aralığında tut."
            app:layout_constraintTop_toBottomOf="@id/txtTipLabel"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Eğlenceli not -->
        <TextView
            android:id="@+id/txtNote"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Not: Bu tarif Bdino Coffee baristaları tarafından test edilmiştir 🙂"
            app:layout_constraintTop_toBottomOf="@id/txtTip"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Paylaş butonu -->
        <Button
            android:id="@+id/btnShare"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="16dp"
            android:text="Tarifi Paylaş"
            app:layout_constraintTop_toBottomOf="@id/txtNote"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>