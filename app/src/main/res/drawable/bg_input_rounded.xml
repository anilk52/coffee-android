<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Arka plan rengi (senin bej tonun gibi olsun istersen değiştiririz) -->
    <solid android:color="@color/coffee_cream_white" />

    <!-- Kenar yuvarlaklığı -->
    <corners android:radius="16dp" />

    <!-- İç boşluk (EditText’in içinde yazı rahat dursun diye) -->
    <padding
        android:left="8dp"
        android:top="4dp"
        android:right="8dp"
        android:bottom="4dp" />

    <!-- İstersen ince bir çerçeve de ekleyebilirsin -->
    <!--
    <stroke
        android:width="1dp"
        android:color="@color/coffee_latte_brown" />
    -->
</shape>