<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
    android:label="Coffee Demo"
    android:icon="@mipmap/ic_launcher"
    android:theme="@style/Theme.Coffee">

    <!-- app/src/main/AndroidManifest.xml, <application> içinde -->
<activity android:name=".ui.RecipesActivity" />
<activity android:name=".ui.RecipeDetailActivity" />
<!-- AndroidManifest.xml, application etiketinin içine -->
<activity android:name=".RecipesActivity" android:exported="false"/>

    <activity
        android:name=".SplashActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>

    <activity
        android:name=".MainActivity"
        android:exported="true" />
</application>
</manifest>
