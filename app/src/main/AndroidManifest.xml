<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Bdino Coffee"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coffee">

        <!-- LAUNCHER (intent-filter var => exported=true) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Diğer ekranlar (intent-filter yok => exported=false) -->
        <activity
            android:name=".ui.RecipeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.RecipeDetailActivity"
            android:exported="false" />

        <!-- Splash kullanmıyoruz; eklemek istersen exported durumuna göre ekleriz -->
        <!-- <activity android:name=".SplashActivity" android:exported="false" /> -->

    </application>
</manifest>